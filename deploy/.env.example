# Optional legacy file — not required.
# The API uses ArkifiHotel/appsettings.json + appsettings.Production.json instead.
#
# Copy to deploy/.env only if you prefer environment variables over appsettings.

PUBLIC_HOST=31.97.116.169
API_PORT=9001
